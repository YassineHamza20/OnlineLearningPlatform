import ProfileField from "./ProfileField";

function TeachingStyle(props) {
    return (
        <ProfileField title={props.title} placeholder={props.placeholder} icon={props.icon}></ProfileField>
    );
}

export default TeachingStyle;